"""Unified spectrum and repository-file search feature.

Routes render the search experience and downloads. The service module owns the
summary-first SQL strategy, live-catalog fallback, and short-lived query cache.
"""
